{
  "name": "origin-probe-measure",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Trusted Origin G2R measurement endpoint — HMAC share tokens, server-side qualification",
  "scripts": {
    "test": "node --test test/*.test.js",
    "acceptance": "node scripts/acceptance.mjs",
    "start": "node scripts/local-server.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
