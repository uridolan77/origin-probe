name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - name: Build static export for GitHub Pages
        env:
          ORIGIN_PAGES_BASE_PATH: /origin-probe
          SITE_URL: https://uridolan77.github.io/origin-probe
          ORIGIN_BUILD_COMMIT: ${{ github.sha }}
          # Public build-time binding only — no server secrets.
          NEXT_PUBLIC_ORIGIN_MEASUREMENT_API_URL: https://origin-probe-measure.vercel.app
          NEXT_PUBLIC_ORIGIN_LIVE_ANALYTICS: "false"
        run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
