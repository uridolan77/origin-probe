name: Origin G2 event mirror (retired)

# Retired under ORIGIN_G2_WINDOW_001 INVALID_MEASUREMENT.
# Forgeable ntfy transport must not be used for decision-grade measurement.
# Kept for manual forensic replay only (workflow_dispatch).

on:
  workflow_dispatch:

permissions:
  issues: write
  contents: read

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse automatic mirror
        run: |
          echo "ORIGIN_G2_WINDOW_001 is INVALID_MEASUREMENT."
          echo "ntfy mirror is retired. Use G2R trusted measurement endpoint."
          exit 1
